# Add these values in Streamlit Community Cloud: App settings > Secrets.
# Never commit the real key or paste it in chat.
SUPABASE_URL = "https://your-project-ref.supabase.co"
SUPABASE_SECRET_KEY = "sb_secret_replace_with_your_server_side_secret_key"
SUPABASE_BUCKET = "jovi-quality-data"
